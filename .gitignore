# nix build artifacts
result
result-*

# cargo build artifacts (`just cargo build` / `just audit` outside the nix build)
rust/target/

# local runtime / accidental plaintext (the *encrypted* secrets/*.yaml IS committed)
*.plain
/secrets/*.plaintext
.direnv/

# age private keys must never be committed (they decrypt the token)
keys.txt
*.agekey
