# guardrails-allow.txt — prefixes/paths exempted from the gates.
#
# EMPTY. Every Markdown surface is now generated or explicitly wrapped at its
# home (docs-from-code, #23/#24). Adding a line here is a regression, not a fix.
